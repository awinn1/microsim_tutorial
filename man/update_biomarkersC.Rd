% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{update_biomarkersC}
\alias{update_biomarkersC}
\title{Vectorized Biomarker and Lag Variable Update (C++)}
\usage{
update_biomarkersC(
  m_ind_traits,
  m_coef_ukpds_ind_traits,
  m_coef_ukpds_other_ind_traits
)
}
\arguments{
\item{m_ind_traits}{A numeric matrix of patient characteristics. This matrix
serves as both input (containing values from the previous cycle) and output
(it is modified in-place with updated values for the current cycle).}

\item{m_coef_ukpds_ind_traits}{A matrix of the main model coefficients. The
function will use the first 8 columns for its predictions.}

\item{m_coef_ukpds_other_ind_traits}{A matrix of ancillary parameters. The
function will use the first row of the first 8 columns as the \code{lambda}
(intercept) values.}
}
\value{
The updated \code{m_ind_traits} matrix with new values for biomarkers, lag
variables, and transformed variables for the current simulation cycle.
}
\description{
Updates the biomarker values for a cohort of individuals for a single time
step. This function performs three main tasks in sequence:
\enumerate{
\item Updates lag variables (e.g., \code{a1c_lag}) with the current biomarker values.
\item Predicts new values for a set of 8 core biomarkers using a linear model.
\item Computes several transformations based on these new biomarker values (e.g.,
\code{sbp_real} to \code{sbp}, creating BMI categories).
}
}
\details{
\strong{CRITICAL:} This function is highly optimized and operates on the assumption
of a fixed matrix structure. It uses hardcoded column indices to read,
update, and transform values. The input matrix \code{m_ind_traits} \strong{must}
conform to the specific column order that the model expects. Modifying the
input matrix structure without updating the internal indices in this function
will lead to incorrect calculations.
}
